name: khatwa_mobile
description: خطوة - منصة إدارة تعليمية ذكية للطلاب ذوي الاحتياجات الخاصة
version: 1.0.0
publish_to: 'none'

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.0
  flutter_secure_storage: ^9.2.0
  firebase_messaging: ^15.0.0
  firebase_core: ^3.0.0
  intl: ^0.19.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
